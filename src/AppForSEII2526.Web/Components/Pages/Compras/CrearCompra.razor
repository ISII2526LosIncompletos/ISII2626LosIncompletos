@page "/compras/crearcompra"

@inject NavigationManager navigation
@using AppForSEII2526.Web.Components.Shared
@using AppForSEII2526.Web.API
@inject AppForSEII2526APIClient apiclient
@inject ILogger<CrearCompra> Logger
@inject ComprarStateContainer comprarState


<h3>Crear Compra</h3>

@* La compra es el objeto que se edita gracias al formulario *@
<EditForm Model="compra" OnValidSubmit="OpenDialog">
    <div class="row">
        <DataAnnotationsValidator />
        <ValidationSummary class="row alert alert-danger"/>
    </div>
    <div class="row alert alert-danger" role="alert" hidden="@hideErrors">
        <p id="ErrorsShown">Errores: @messageError</p>
    </div>
    <div class="row g-3">
        <div class="col-md">
            <div class="form-floating mb-3">
                <InputText @bind-Value="compra.NombreCliente" class="form-control" id="NombreCliente" />
                <label for="NombreCliente">Nombre</label>
            </div>
        </div>
        <div class="col-md">
            <div class="form-floating mb-3">
                <InputText @bind-Value="compra.ApellidoCliente" class="form-control" id="ApellidoCliente" />
                <label for="ApellidosCliente">Apellidos</label>
            </div>
        </div>
        <div class="col-md">
            <div class="form-floating mb-3">
                <InputDate @bind-Value="compra.DireccionEnvio" class="form-control" id="DirecciónEnvío" />
                <label for="FechaEntrega">Dirección de envío</label>
            </div>
        </div>
    </div>
    <div class=" row g-2">
        <div class=" col-md">
            <div class="form-floating">
            @*Un menú desplegable para seleccionar el método de pago*@
            <InputSelect class="form-select" @bind-Value="@compra.MetodoPago" id="MetodoPago">
                <option value="@TiposMetodosPago.TarjetaCredito" default> Tarjeta de crédito</option>
                <option value="@TiposMetodosPago.PayPal"> PayPal</option>
                <option value="@TiposMetodosPago.Efectivo"> Efectivo</option>
            </InputSelect>
            <label for="PaymentMethod">Método de Pago</label>
            </div>
         </div>
        <div class="col-md">
            <div class="form-floating">
                @*Este botón es para preguntar al usuario si quiere proceder a comprar una herramienta*@
                <button class="btn btn-primary" type="submit" id="Submit">
                    Comprar herramientas
                </button>
            </div>
        </div>
    </div>

    <h4>Detalles Compra</h4>
    <p>
        <b>Precio Total:</b> @PrecioTotal€
        <button class="btn btn-outline-primary" type="submit" @onclick="ModifyItemsCompra" id="ModifyHerramientas">
            Modificar herramientas
        </button>
    </p>
    <div class="mh-100 table-responsive">
        <table class="table table-consdensed table-hover" id="TableOfItemsCompras">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Material</th>
                    <th>Precio de compra</th>
                    <th>Descripción</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in compra.CompraItems)
                {
                    <tr id="HerramientaData_@item.Nombre">
                        <td>@item.Nombre</td>
                        <td>@item.Material</td>
                        <td>@item.PrecioCompra</td>
                        <td><InputText @bind-Value="item.Descripcion" class="form-control" id="@($"description_{item.HerramientaID}")" /></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</EditForm>


@*Un cuadro de diálogo para preguntar al usuario si desea proceder con comprar herramientas*@
@if (DialogIsOpen)
{
    <Dialog Caption="Comprar herramientas"
            Message="¿Quieres proceder a la compra de las herramientas seleccionadas?"
            OnClose="@OnDialogClose"
            Type=Dialog.Category.SaveNot>
    </Dialog>
}
 
@code {
    private string messageError = "";
    private bool hideErrors { get { return messageError.Length == 0; } }
    private bool DialogIsOpen;

    private decimal PrecioTotal
    {
        get
        {
            double precio = compra.CompraItems.Sum(ri => ri.PrecioCompra);
            return Convert.ToDecimal(precio);
        }
    }
    private void ModifyItemsCompra()
    {
        navigation.NavigateTo("/compras/seleccionarherramientasparacomprar");
    }

    private CompraCreacionDTO compra => comprarState.Compra;

    private void OpenDialog()
    {
        DialogIsOpen = true;
    }

    private async Task OnDialogClose(bool isOk)
    {

        DialogIsOpen = false;
        if (isOk)
        {
            // Process the data introduced by the user if they are valid
            if (compra.CompraItems.Count > 0)
            {
                try
                {
                    var comprado = await apiclient.CreateCompraAsync(compra);
                    comprarState.FinalizarCompra();
                    navigation.NavigateTo($"/compras/compradetalle/?compraID={comprado.CompraId}");
                }
                catch(ApiException<ValidationProblemDetails> apiexceptionvalidation)
                {
                    if (apiexceptionvalidation.Result.Errors.Values != null)
                    {
                        messageError = "";
                        foreach (var errors in apiexceptionvalidation.Result.Errors.Values)
                        {
                            //https://learn.microsoft.com/en-us/dotnet/api/system.string.join?view=net-7.0
                            messageError = string.Join("(*) ", messageError, string.Join(" (*) ", errors));
                        }
                    }
                }
                catch (ApiException apiexception)
                {
                    //Necesitamos informar al usuario si se produjeron errores al procesar la solicitud
                    messageError = "Error mientras se procesaba la compra, por favor, pruebe más tarde!";

                }
            }

        }
    }

}

   